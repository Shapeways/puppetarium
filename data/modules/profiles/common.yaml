---
lookup_options:
  profiles::example::data:
    merge:
      strategy: deep
lookup_options:
  profiles::config_files::data:
    merge:
      strategy: deep
  
profiles::config_files::params::data:
    num_processes: '5'
    interfaces:
      eth0:
        type: one
      eth1:
        type: standard
      p3p4:
        type: one
      em1:
        type: more
  
profiles::example::data:
    key: "value"
