#!/bin/bash
puppet apply --verbose --codedir /data/puppetarium/ --modulepath /data/puppetarium/modules --fileserverconfig=/data/puppetarium/fileserver.conf /data/puppetarium/environments/production/manifests/site/
